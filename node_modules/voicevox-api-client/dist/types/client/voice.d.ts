import type { Client } from './index.js';
import type { Query } from '../types/index.js';
export declare class VoiceClient {
    private readonly client;
    constructor(client: Client);
    createVoice(speaker: number, query: Query): Promise<ArrayBuffer>;
    createVoiceCancellable(speaker: number, query: Query): Promise<Response>;
    createVoiceMulti(speaker: number, queries: Query[]): Promise<ArrayBuffer>;
    createVoiceMorphing(base_speaker: number, target_speaker: number, morph_rate: number, query: Query): Promise<ArrayBuffer>;
}
