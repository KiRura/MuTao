import type { Client } from './index.js';
import type { Preset, SpeakerData, SpeakerInfo } from '../types/index.js';
export declare class OtherClient {
    private readonly client;
    constructor(client: Client);
    margeWav(base64Array: string[]): Promise<ArrayBuffer>;
    getPreset(): Promise<Preset>;
    getVersion(): Promise<string>;
    getSpeakers(): Promise<SpeakerData[]>;
    getSpeakerInfo(speaker_uuid: string): Promise<SpeakerInfo>;
}
