import type { Client } from './index.js';
import type { Mora, Query } from '../types/index.js';
export declare class QueryClient {
    private readonly client;
    constructor(client: Client);
    createQuery(speaker: number, text: string): Promise<Query>;
    createQueryWithPreset(preset: number, text: string): Promise<Query>;
    getAccentPhrases(speaker: number, text: string, is_kana?: boolean): Promise<Query['accent_phrases'][]>;
    getPitchAndPhonemeLength(speaker: number, moras: Mora[], accent: number, pause_mora?: Mora): Promise<Query['accent_phrases'][]>;
    getPhonemeLength(speaker: number, moras: Mora[], accent: number, pause_mora?: Mora): Promise<Query['accent_phrases'][]>;
    getPitch(speaker: number, moras: Mora[], accent: number, pause_mora?: Mora): Promise<Query['accent_phrases'][]>;
}
